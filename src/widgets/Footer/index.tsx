import * as i from "./imports";

export const Footer = () => {
  const router = i.useNavigate();

  return (
    <footer
      className={` w-full items-center flex justify-center text-white breal-all`}
    ></footer>
  );
};
